---
import ProjectDetailScreen from "../../../screens/ProjectDetailScreen.astro";
import { getProjectStaticPaths } from "../../../screens/projectPaths";

export const getStaticPaths = getProjectStaticPaths("fr");

const { project } = Astro.props;
---

<ProjectDetailScreen locale="fr" project={project} />
